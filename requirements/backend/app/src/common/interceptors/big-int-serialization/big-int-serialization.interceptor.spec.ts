import { BigIntSerializationInterceptor } from './big-int-serialization.interceptor';

describe('BigIntSerializationInterceptor', () => {
  it('should be defined', () => {
    expect(new BigIntSerializationInterceptor()).toBeDefined();
  });
});
